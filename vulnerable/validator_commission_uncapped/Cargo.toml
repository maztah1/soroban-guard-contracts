[package]
name = "validator_commission_uncapped"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban validator contract with uncapped commission"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
