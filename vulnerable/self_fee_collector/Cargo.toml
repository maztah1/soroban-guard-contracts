[package]
name = "self-fee-collector"
version = "0.1.0"
edition = "2021"
description = "Vulnerable Soroban contract: fee collector can be set to the contract itself, trapping fees"
license = "MIT"
repository = "https://github.com/Veritas-Vaults-Network/soroban-guard-contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
